[project]
name = "uv-disk-manager"
version = "0.1.0"
description = "CLI tool for monitoring and cleaning up disk space used by uv Python packages"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["rich>=13.0.0"]
